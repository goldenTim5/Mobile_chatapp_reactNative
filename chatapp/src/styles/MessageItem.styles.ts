import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginVertical: 8,
    padding: 12,
    backgroundColor: '#f0f0f0',
    borderRadius: 12,
  },
  text: {
    fontSize: 16,
    color: '#333',
  },
  image: {
    marginTop: 8,
    borderRadius: 8,
  },
});